% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types_deribit.R
\name{deribit_shapes}
\alias{deribit_shapes}
\title{deribit return shapes}
\description{
Reusable roxyassert \verb{@type} shapes for the parsed deribit
\code{data.table}s. Measurement columns (market prices, sizes, greeks, funding, and
any venue field that is legitimately absent for a given instrument kind) are
typed \verb{| NA}; structural columns (identifiers, instrument descriptors, sides,
period timestamps, and flags) are strict. Faithful field names are preserved
verbatim and only snake_cased; nested JSON objects are flattened with the
parent field as a prefix (\code{stats.high} -> stats_high, \code{greeks.delta} ->
greeks_delta). \code{deribit} is a leaf connector: nothing internal calls a
per-shape validator and no downstream package validates against these shapes,
so there is no \verb{@genassert} and no \verb{@exportassert}; the shapes exist only to be
expanded into each method's own return contract.
}

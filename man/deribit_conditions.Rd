% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{deribit_conditions}
\alias{deribit_conditions}
\title{Typed deribit conditions}
\description{
\code{deribit} raises \strong{classed conditions} so a caller branches on error \emph{type}
and reads structured \emph{fields} instead of matching the message text.
}
\details{
\subsection{Class taxonomy}{
\itemize{
\item \strong{Transport / API failures} nest specific -> general as
\verb{deribit_api_error_<status>} -> \code{deribit_api_error} ->
\verb{connectcore_api_error_<status>} -> \code{connectcore_api_error} ->
\code{connectcore_error}, carrying the fields \code{status} (the HTTP status),
\code{code} (Deribit's JSON-RPC error code, e.g. \code{-32602}), \code{reason} and \code{param}
(the JSON-RPC \code{error.data} detail, when present), \code{url}, and \code{body_snippet}.
Raised whenever a Deribit response carries a JSON-RPC \code{error} object (which
arrives with an HTTP 400), or for any other non-2xx HTTP status.
\item \strong{Validation failures} nest \code{deribit_validation_error} -> \code{deribit_error}
(the domain root). Raised for a malformed argument (e.g. an unknown
\code{resolution} or \code{kind}, or \code{end_timestamp} before \code{start_timestamp}) before
any request is made.
}

Deribit signs no public request, so no request URL carries a secret; the \code{url}
is nonetheless stored through \code{\link[connectcore:scrub_url]{connectcore::scrub_url()}} for fleet uniformity,
so any future authenticated surface inherits the redaction for free.
}
}
\seealso{
\link[connectcore:connectcore_conditions]{connectcore::connectcore_conditions}
}

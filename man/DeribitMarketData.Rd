% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DeribitMarketData.R
\name{DeribitMarketData}
\alias{DeribitMarketData}
\title{DeribitMarketData: Deribit Public Market-Data Client}
\description{
The R6 client for Deribit's public market-data surface — the instrument
universe, live quotes, the DVOL volatility index, perpetual funding, public
trades, and option-chain book summaries. Every method speaks Deribit's
JSON-RPC 2.0 over HTTP GET and returns a tidy \link[data.table:data.table]{data.table::data.table} typed
per \link{deribit_shapes}.
}
\details{
The class inherits the generic transport (the single sync/async request
funnel, retry, throttle) from \link[connectcore:RestClient]{connectcore::RestClient} and customises only the
\code{.parse_envelope()} seam (via the internal \code{parse_deribit_response()}), which
unwraps the JSON-RPC \code{result} and raises a typed \link{deribit_conditions} error
when the response carries an \code{error} object.
\subsection{Sync vs async}{

The \code{async} argument selects the execution mode for every method:
\itemize{
\item \code{async = FALSE} (default): methods return a \link[data.table:data.table]{data.table::data.table}.
\item \code{async = TRUE}: methods return a \link[promises:promise]{promises::promise} that resolves to the
same \code{data.table}.
}

The mode is stored once as \code{private$.is_async} and threaded through every
method's \code{connectcore::then_or_now(res, ..., is_async = private$.is_async)}
tail; nothing hardcodes it. Consume promises with \code{\link[coro:async]{coro::async()}} and \code{await()};
drive the loop in a script with \code{while (!later::loop_empty()) later::run_now()}.
}

\subsection{No API key}{

The \verb{public/*} endpoints require no credential, so the client is constructed
with no key. \code{max_tries} opts idempotent GET reads into retry on a transient
failure (public reads are GET, so retry is always safe here).
}
}
\examples{
\dontrun{
md <- DeribitMarketData$new()

# The option universe and a single chain snapshot:
options <- md$get_instruments("BTC", kind = "option")
chain <- md$get_book_summary_by_currency("BTC", kind = "option")

# The DVOL volatility index (the regime dial):
dvol <- md$get_volatility_index_data(
  "BTC",
  start_timestamp = lubridate::now("UTC") - lubridate::days(7),
  end_timestamp = lubridate::now("UTC"),
  resolution = "3600"
)

# Perpetual funding:
funding <- md$get_funding_rate_history(
  "BTC-PERPETUAL",
  start_timestamp = lubridate::now("UTC") - lubridate::days(7),
  end_timestamp = lubridate::now("UTC")
)

# Asynchronous:
md_async <- DeribitMarketData$new(async = TRUE)
main <- coro::async(function() {
  ticker <- await(md_async$get_ticker("BTC-PERPETUAL"))
  print(ticker)
})
main()
while (!later::loop_empty()) later::run_now()
}

}
\section{Super class}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{DeribitMarketData}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-DeribitMarketData-new}{\code{DeribitMarketData$new()}}
\item \href{#method-DeribitMarketData-get_instruments}{\code{DeribitMarketData$get_instruments()}}
\item \href{#method-DeribitMarketData-get_currencies}{\code{DeribitMarketData$get_currencies()}}
\item \href{#method-DeribitMarketData-get_ticker}{\code{DeribitMarketData$get_ticker()}}
\item \href{#method-DeribitMarketData-get_order_book}{\code{DeribitMarketData$get_order_book()}}
\item \href{#method-DeribitMarketData-get_index_price}{\code{DeribitMarketData$get_index_price()}}
\item \href{#method-DeribitMarketData-get_volatility_index_data}{\code{DeribitMarketData$get_volatility_index_data()}}
\item \href{#method-DeribitMarketData-get_volatility_index_data_raw}{\code{DeribitMarketData$get_volatility_index_data_raw()}}
\item \href{#method-DeribitMarketData-get_funding_rate_history}{\code{DeribitMarketData$get_funding_rate_history()}}
\item \href{#method-DeribitMarketData-get_funding_rate_value}{\code{DeribitMarketData$get_funding_rate_value()}}
\item \href{#method-DeribitMarketData-get_last_trades_by_instrument}{\code{DeribitMarketData$get_last_trades_by_instrument()}}
\item \href{#method-DeribitMarketData-get_book_summary_by_currency}{\code{DeribitMarketData$get_book_summary_by_currency()}}
\item \href{#method-DeribitMarketData-get_book_summary_by_currency_raw}{\code{DeribitMarketData$get_book_summary_by_currency_raw()}}
\item \href{#method-DeribitMarketData-get_book_summary_by_instrument}{\code{DeribitMarketData$get_book_summary_by_instrument()}}
\item \href{#method-DeribitMarketData-get_option_chain}{\code{DeribitMarketData$get_option_chain()}}
\item \href{#method-DeribitMarketData-get_option_chain_raw}{\code{DeribitMarketData$get_option_chain_raw()}}
\item \href{#method-DeribitMarketData-clone}{\code{DeribitMarketData$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-new"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-new}{}}}
\subsection{Method \code{new()}}{
Initialise a DeribitMarketData client.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$new(
  base_url = deribit_base_url(),
  async = FALSE,
  max_tries = 1L,
  throttle_rate = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{base_url}}{(scalar<character>) the Deribit API base URL. Defaults to
\code{\link[=deribit_base_url]{deribit_base_url()}}.}

\item{\code{async}}{(scalar<logical>) if \code{TRUE}, methods return promises. Default
\code{FALSE}.}

\item{\code{max_tries}}{(scalar<count in [1, Inf[>) retry an idempotent GET up to
this many times on a transient failure (408, 429, any 5xx, or a connection
failure). Every public read is a GET, so retry is always safe. Default \code{1}
(no retry).}

\item{\code{throttle_rate}}{(scalar<numeric in ]0, Inf[> | NULL) client-side rate
cap in requests/second. Default \code{NULL} (no throttle).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(class<DeribitMarketData>) invisibly, self.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_instruments"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_instruments}{}}}
\subsection{Method \code{get_instruments()}}{
Retrieve the tradeable instrument universe for a currency.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_instruments(currency, kind = NULL, expired = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}

\item{\code{kind}}{(scalar<character> | NULL) the instrument-kind filter, one of
\code{names(INSTRUMENT_KIND)} ("future", "option", "spot", "future_combo",
"option_combo"); \code{NULL} returns every kind. Default \code{NULL}.}

\item{\code{expired}}{(scalar<logical>) if \code{TRUE}, return expired instruments
instead of the live universe. Default \code{FALSE}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(Instruments | promise<Instruments>) one row per instrument, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_currencies"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_currencies}{}}}
\subsection{Method \code{get_currencies()}}{
List the currencies Deribit supports.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_currencies()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
(Currencies | promise<Currencies>) one row per currency, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_ticker"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_ticker}{}}}
\subsection{Method \code{get_ticker()}}{
Retrieve the combined stats/quote ticker for one instrument.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_ticker(instrument_name)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{instrument_name}}{(scalar<character>) the instrument, e.g.
\code{"BTC-PERPETUAL"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(Ticker | promise<Ticker>) a one-row ticker table, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_order_book"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_order_book}{}}}
\subsection{Method \code{get_order_book()}}{
Retrieve the order book for one instrument as a long
(one-row-per-level) table.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_order_book(instrument_name, depth = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{instrument_name}}{(scalar<character>) the instrument, e.g.
\code{"BTC-PERPETUAL"}.}

\item{\code{depth}}{(scalar<count in [1, Inf[> | NULL) the number of price levels
per side; \code{NULL} uses the venue default. Default \code{NULL}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(OrderBook | promise<OrderBook>) one row per price level (bids then
asks), or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_index_price"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_index_price}{}}}
\subsection{Method \code{get_index_price()}}{
Retrieve the current value of a price index.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_index_price(index_name)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{index_name}}{(scalar<character>) the index name, e.g. \code{"btc_usd"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(IndexPrice | promise<IndexPrice>) a one-row index-price table, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_volatility_index_data"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_volatility_index_data}{}}}
\subsection{Method \code{get_volatility_index_data()}}{
Retrieve DVOL volatility-index candles over a time window. A
single call returns up to ~1000 candles; page a longer range by moving
\code{end_timestamp} back to the earliest returned candle's time.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_volatility_index_data(
  currency,
  start_timestamp,
  end_timestamp,
  resolution
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}

\item{\code{start_timestamp}}{(class<POSIXct>) the window start (inclusive).}

\item{\code{end_timestamp}}{(class<POSIXct>) the window end (inclusive). Must not
precede \code{start_timestamp}.}

\item{\code{resolution}}{(scalar<character>) the candle resolution, one of
\code{DVOL_RESOLUTIONS} ("1", "60", "3600", "43200" seconds, or "1D" daily).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(DvolCandles | promise<DvolCandles>) one row per candle, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_volatility_index_data_raw"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_volatility_index_data_raw}{}}}
\subsection{Method \code{get_volatility_index_data_raw()}}{
Retrieve a DVOL window as the venue's own \strong{raw object} — the
parsed JSON \verb{\{ data, continuation \}} exactly as Deribit returns it. This
is the lossless counterpart to \code{get_volatility_index_data()}: that method
returns tidy candle rows and drops the paging cursor, whereas this
preserves both the raw \verb{[timestamp_ms, open, high, low, close]} \code{data}
rows and the \code{continuation} cursor Deribit returns when more history
remains (its next \code{end_timestamp} in ms, or JSON \code{null} when the range is
exhausted). A backfill pages a range longer than one response by feeding
\code{continuation} back as the next \code{end_timestamp}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_volatility_index_data_raw(
  currency,
  start_timestamp,
  end_timestamp,
  resolution
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}

\item{\code{start_timestamp}}{(class<POSIXct>) the window start (inclusive).}

\item{\code{end_timestamp}}{(class<POSIXct>) the window end (inclusive). Must not
precede \code{start_timestamp}.}

\item{\code{resolution}}{(scalar<character>) the candle resolution, one of
\code{DVOL_RESOLUTIONS} ("1", "60", "3600", "43200" seconds, or "1D" daily).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(list | promise<list>) the raw \verb{\{ data, continuation \}} object, or
a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_funding_rate_history"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_funding_rate_history}{}}}
\subsection{Method \code{get_funding_rate_history()}}{
Retrieve the funding-rate history for a perpetual over a
window.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_funding_rate_history(
  instrument_name,
  start_timestamp,
  end_timestamp
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{instrument_name}}{(scalar<character>) the perpetual, e.g.
\code{"BTC-PERPETUAL"}.}

\item{\code{start_timestamp}}{(class<POSIXct>) the window start (inclusive).}

\item{\code{end_timestamp}}{(class<POSIXct>) the window end (inclusive). Must not
precede \code{start_timestamp}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(FundingRateHistory | promise<FundingRateHistory>) one row per
sample, or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_funding_rate_value"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_funding_rate_value}{}}}
\subsection{Method \code{get_funding_rate_value()}}{
Retrieve the aggregate funding rate for a perpetual over a
window (the current funding when the window is the last 8 hours).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_funding_rate_value(
  instrument_name,
  start_timestamp,
  end_timestamp
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{instrument_name}}{(scalar<character>) the perpetual, e.g.
\code{"BTC-PERPETUAL"}.}

\item{\code{start_timestamp}}{(class<POSIXct>) the window start (inclusive).}

\item{\code{end_timestamp}}{(class<POSIXct>) the window end (inclusive). Must not
precede \code{start_timestamp}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(FundingRateValue | promise<FundingRateValue>) a one-row aggregate
funding table, or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_last_trades_by_instrument"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_last_trades_by_instrument}{}}}
\subsection{Method \code{get_last_trades_by_instrument()}}{
Retrieve the most recent public trades for one instrument.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_last_trades_by_instrument(
  instrument_name,
  count = NULL,
  start_timestamp = NULL,
  end_timestamp = NULL,
  sorting = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{instrument_name}}{(scalar<character>) the instrument, e.g.
\code{"BTC-PERPETUAL"}.}

\item{\code{count}}{(scalar<count in [1, Inf[> | NULL) the maximum number of trades
to return; \code{NULL} uses the venue default. Default \code{NULL}.}

\item{\code{start_timestamp}}{(class<POSIXct> | NULL) the earliest trade time to
include; \code{NULL} omits the bound. Default \code{NULL}.}

\item{\code{end_timestamp}}{(class<POSIXct> | NULL) the latest trade time to
include; \code{NULL} omits the bound. Default \code{NULL}.}

\item{\code{sorting}}{(scalar<character> | NULL) the sort order, one of
\code{TRADE_SORTING} ("asc", "desc", "default"); \code{NULL} uses the venue default.
Default \code{NULL}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(Trade | promise<Trade>) one row per trade, or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_book_summary_by_currency"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_book_summary_by_currency}{}}}
\subsection{Method \code{get_book_summary_by_currency()}}{
Retrieve the book summary for every live instrument of a
currency (with \code{kind = "option"} this is the option-chain snapshot the
fleet consumes).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_book_summary_by_currency(currency, kind = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}

\item{\code{kind}}{(scalar<character> | NULL) the instrument-kind filter, one of
\code{names(INSTRUMENT_KIND)} ("future", "option", "spot", "future_combo",
"option_combo"); \code{NULL} returns every kind. Default \code{NULL}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(BookSummary | promise<BookSummary>) one row per instrument, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_book_summary_by_currency_raw"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_book_summary_by_currency_raw}{}}}
\subsection{Method \code{get_book_summary_by_currency_raw()}}{
Retrieve the book summary for every live instrument of a
currency as the venue's own \strong{raw records} — the parsed JSON array
exactly as Deribit returns it, in venue order, with every field preserved
(including \code{creation_timestamp}, which the typed table derives away) and a
JSON \code{null} kept distinct from an absent field. This is the lossless
counterpart to \code{get_book_summary_by_currency()}: where that method returns
a tidy typed \code{data.table}, this returns the untouched records a bronze
passthrough archives verbatim. Each element is one instrument's book
summary as a named list.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_book_summary_by_currency_raw(currency, kind = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}

\item{\code{kind}}{(scalar<character> | NULL) the instrument-kind filter, one of
\code{names(INSTRUMENT_KIND)} ("future", "option", "spot", "future_combo",
"option_combo"); \code{NULL} returns every kind. Default \code{NULL}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(list | promise<list>) the raw book-summary records (one named list
per instrument), or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_book_summary_by_instrument"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_book_summary_by_instrument}{}}}
\subsection{Method \code{get_book_summary_by_instrument()}}{
Retrieve the book summary for one instrument.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_book_summary_by_instrument(instrument_name)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{instrument_name}}{(scalar<character>) the instrument, e.g.
\code{"BTC-PERPETUAL"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(BookSummary | promise<BookSummary>) a one-row book-summary table,
or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_option_chain"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_option_chain}{}}}
\subsection{Method \code{get_option_chain()}}{
Retrieve the option-chain snapshot for a currency. A
convenience wrapper over \code{get_book_summary_by_currency(currency, kind = "option")} — the one Deribit call the scraper's option-chain
collector consumes.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_option_chain(currency)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(BookSummary | promise<BookSummary>) one row per live option, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-get_option_chain_raw"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-get_option_chain_raw}{}}}
\subsection{Method \code{get_option_chain_raw()}}{
Retrieve the option-chain snapshot for a currency as the
venue's own \strong{raw records} (the lossless counterpart to
\code{get_option_chain()}): a convenience wrapper over
\code{get_book_summary_by_currency_raw(currency, kind = "option")} — the one
Deribit call the scraper's option-chain collector archives verbatim as
bronze.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$get_option_chain_raw(currency)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) the currency, e.g. \code{"BTC"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(list | promise<list>) the raw option book-summary records (one
named list per live option), or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DeribitMarketData-clone"></a>}}
\if{latex}{\out{\hypertarget{method-DeribitMarketData-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DeribitMarketData$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}

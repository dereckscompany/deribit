# Trade sort order

The `sorting` values the trade endpoints accept: ascending, descending,
or the venue default (no explicit sort).

## Usage

``` r
TRADE_SORTING
```

## Format

A named `list` of `scalar<character>` wire tokens: `asc`, `desc`,
`default`.
